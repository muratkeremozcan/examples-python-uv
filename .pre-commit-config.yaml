repos:
  - repo: local
    hooks:
      - id: make-validate
        name: make validate
        entry: bash -c "make validate && git add -A"
        language: system
        pass_filenames: false
      - id: pytest
        name: uv run pytest
        entry: uv run pytest
        language: system
        pass_filenames: false
